import { Not-found.component } from './not-found.component';

describe('Not-found.component', () => {
  let instance: Not-found.component;

  beforeEach(() => {
    instance = new Not-found.component();
  });

  it('should create an instance', () => {
    expect(instance).toBeTruthy();
  });
});